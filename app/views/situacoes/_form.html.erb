<%= form_with(model: situacao, local: true) do |form| %>
  <% if situacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(situacao.errors.count, "error") %> prohibited this situacao from being saved:</h2>

      <ul>
      <% situacao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :situacao_nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
